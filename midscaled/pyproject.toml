[project]
name = "midscaled"
version = "0.1.0"
description = "Midscale client daemon — WireGuard tunnel manager"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.27.0",
    "structlog>=24.1.0",
]

[project.scripts]
midscaled = "daemon.main:main"
